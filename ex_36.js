function makeShirt(size, message) {
    console.log("Your ".concat(size, "-sized shirt will have the following message: '").concat(message, "'."));
}
// Example usage:
makeShirt("M", "I love TypeScript!");
