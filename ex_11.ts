// Store the names of friends in an array called names
const names: string[] = ["Zain", "Asif", "Charlie", "Hamza"];

// Print each person's name by accessing each element in the list, one at a time
for (let i = 0; i < names.length; i++) {
    console.log(names[i]);
}
